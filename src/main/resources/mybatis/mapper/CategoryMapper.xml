<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sut.school_mall.mapper.CategoryMapper">
    <select id="getAllCategory" resultType="com.sut.school_mall.bean.Category">
        SELECT * FROM category WHERE store_name = #{storeName}
    </select>
</mapper>